
{
  "name": "novacommerce",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "dev": "echo \"Workspace is not configured yet\"",
    "build": "echo \"Build is not configured yet\"",
    "lint": "echo \"Lint is not configured yet\"",
    "test": "echo \"Tests are not configured yet\""
  }
}
